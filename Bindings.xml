<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
    WoW auto-loads <Addon>/Bindings.xml; the file must exist or the client
    warns "Couldn't open .../Bindings.xml". The wind-call buttons are secure
    macro buttons and need no key bindings, so this file is intentionally
    empty.
-->
<Bindings>
</Bindings>
